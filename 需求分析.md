# 今日微光（Tiny Light）需求分析文档

> 版本：v1.0 ｜ 日期：2026-07-22 ｜ 状态：MVP 规划

---

## 1. 项目概述

### 1.1 背景
人们在日常中常忽略自己经历过的微小美好，低谷时更容易陷入"我好像一直都不好"的错觉。市面上的情绪类 App 大多让人记录"今天有多糟"，而「今日微光」反其道而行——只记录"今天最亮的一刻"，把一年的好瞬间串成一条"星光河"，在用户回头看时被自己过去的光治愈。

### 1.2 产品定位（一句话）
> 每天只记一句"今天最亮的一刻"，系统把 365 天串成一条可回看的星光河；平时轻盈无负担，低谷时回头被治愈。

### 1.3 目标与价值
- **情绪价值**：正向聚焦（积极心理学"三件好事"练习的极简版），坚持成本低、幸福感收益真实。
- **留存钩子**：用得越久，星光河越有价值；"去年今日""连续点亮徽章"形成回访动力。
- **传播性**：年度星光河长图、轻社区"借到光"天然适合分享。

---

## 2. 用户分析

### 2.1 目标用户
- 想要低成本记录生活美好、但坚持不了写长日记的人；
- 有情绪记录/自我疗愈需求、偏好轻量工具的用户；
- 对"可视化、好看、能分享"有审美要求的年轻用户。

### 2.2 用户角色
| 角色 | 说明 | 当前版本 |
|---|---|---|
| 匿名访客 | 无注册，浏览器 `localStorage` 存 `userId` 区分用户 | ✅ MVP 支持 |
| 注册用户 | 邮箱/手机登录、多端同步 | ⏳ 后续版本 |
| 管理员 | 内容审核、社区治理 | ⏳ 轻社区时再考虑 |

> MVP 采用"无登录极简版"：不注册、不登录，最快出可玩 demo，先把核心闭环跑通。

### 2.3 典型使用场景
- **睡前 1 分钟**：写一句今天最亮的事 → 点亮当天的星。
- **低谷时**：打开星光河，看过去被点亮的日子，被治愈。
- **年末**：导出星光河长图发朋友圈；收到 AI 写给自己的"年度微光信"。
- **日常**：收到"去年今日，你写过……"的温柔提醒。

---

## 3. 功能需求

### 3.1 功能清单
| 编号 | 功能 | 优先级 | 说明 |
|---|---|---|---|
| F1 | 记录今日微光 | P0 核心 | 写一句文字（可选情绪标签），点亮当天 |
| F2 | 查看今日状态 | P0 核心 | 是否已点亮、今天写了什么 |
| F3 | 年度星光河 | P0 核心 | 某年按天的点阵，金点=已点亮，灰点=空白 |
| F4 | 那年今日 | P1 | 显示往年同月同日的微光 |
| F5 | 微光详情 | P1 | 点开任意一颗星看回那天的记录 |
| F6 | 连续点亮徽章 | P2 | 连更 7/30/100 天成就 |
| F7 | 年度星光河长图 | P2 | 一键生成竖版"我的 2026 星光河"长图 |
| F8 | AI 年度微光信 | P3 | 年末用大模型把全年星光凝成一封信 |
| F9 | 轻社区"借到光" | P3 | 某颗星设为公开，别人可"借到光" |
| F10 | 去年今日推送 | P3 | 定时任务每日推"一年前的你写道…" |

### 3.2 核心功能详述（MVP：F1–F5）

**F1 记录今日微光**
- 用户故事：作为访客，我想写下今天最亮的一刻，这样我能把它存进我的星光河。
- 输入：`userId`（自动）、`content`（必填，≤500 字）、`mood`（可选）。
- 规则：**一人一天只能一颗**；重复提交返回冲突提示。
- 验收：提交后该用户当天 `todayLighted=true`；数据库新增一行；星光河对应日期出现金点。

**F2 查看今日状态**
- 用户故事：作为访客，我想知道今天是否已点亮，避免重复写。
- 验收：返回 `todayLighted` 布尔 + 已写内容（若已点亮）或 `null`。

**F3 年度星光河**
- 用户故事：作为访客，我想看某一年的星光河，回看自己被点亮过的日子。
- 输入：`userId`、`year`。
- 验收：返回该年全部已点亮记录；前端渲染成点阵（金点/灰点），可滚动、渐显。

**F4 那年今日**
- 用户故事：作为访客，我想看"历史上的今天"我写过什么，获得时间对照的温柔。
- 验收：返回近 5 年内、与今天同月同日的微光列表（可空）。

**F5 微光详情**
- 用户故事：作为访客，我想点开某颗星，看回那天写的具体内容。
- 输入：微光 `id`。
- 验收：返回该条完整内容；不存在时返回 404。

### 3.3 扩展功能（Roadmap：F6–F10）
见 3.1 表格，按 P2→P3 优先级在 MVP 跑通后迭代。其中 F8/F10 依赖 **Spring AI**（大模型）与 **@Scheduled**（定时任务）。

---

## 4. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 单用户量极小；星光河查询为单表按日期范围检索，响应 < 200ms；首屏加载 < 1s |
| 易用性 | 输入页极简（大留白 + 引导文案 + 情绪小标签）；零学习成本；移动端适配 |
| 安全 | MVP 无登录，`userId` 由前端生成，后端仅做参数校验；后续版本加 JWT 防越权 |
| 兼容性 | 现代浏览器（Chrome/Edge/Safari 近两版）；响应式布局适配手机 |
| 可维护性 | 后端分层清晰（Controller/Service/Mapper/Entity）；前端组件化；配置与代码分离 |
| 可观测 | 开发期在控制台打印 SQL（`log-impl=StdOutImpl`）；生产可关 |

---

## 5. 数据需求

### 5.1 实体关系
- 当前仅一个核心实体 **微光（TinyLight）**，归属某个 `userId`。
- 关系：`用户(1) —— (N) 微光`。MVP 无独立用户表，`userId` 为前端生成的字符串。

### 5.2 数据表设计（tiny_light）
| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO_INCREMENT | 微光唯一 ID |
| user_id | VARCHAR(64) | NOT NULL | 用户标识（前端生成） |
| content | VARCHAR(500) | NOT NULL | 今日微光文字 |
| mood | VARCHAR(20) | 可空 | 情绪标签（平静/开心/感动…） |
| light_date | DATE | NOT NULL | 点亮日期 |
| created_at | DATETIME | 可空 | 创建时间 |
| — | UNIQUE(user_id, light_date) | — | 保证一人一天一颗 |

### 5.3 数据规则
- **一人一天一颗**：由数据库唯一约束 + 业务层 `selectCount` 双重保证。
- **保留策略**：微光不软删（有情感价值）；后续若做轻社区，公开/私有用独立标记字段。
- **字符上限**：content ≤ 500 字，前端与后端双重校验。

---

## 6. 接口需求（API 规格）

基址：`http://localhost:8080/api/lights`（前端经 Vite 代理 `/api` 访问）

| 方法 | 路径 | 参数 | 返回 | 说明 |
|---|---|---|---|---|
| POST | `/` | body: `{userId, content, mood?}` | `LightResponse` | 记录今日微光；重复→409 |
| GET | `/today` | `userId` | `{todayLighted, light}` | 今日状态 |
| GET | `/river` | `userId`, `year` | `List<LightResponse>` | 年度星光河 |
| GET | `/on-this-day` | `userId` | `List<LightResponse>` | 那年今日 |
| GET | `/{id}` | path: `id` | `LightResponse` | 单颗星详情；无→404 |

**LightResponse 结构**：`{id, userId, content, mood, lightDate, createdAt}`

**错误约定**：业务异常统一返回 `{error: "提示文案"}` + 对应 HTTP 状态码（如重复 409、不存在 404）。

---

## 7. 技术架构与约束

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Vue 3 + Vite | 开发服务器 5173，代理 `/api`→8080，免跨域 |
| 后端 | Spring Boot 3.2 + Java 17 | 提供 REST API、写业务逻辑 |
| 持久层 | **MyBatis-Plus** + MySQL | `BaseMapper` 白送 CRUD，`QueryWrapper` 拼条件；表由 `schema.sql` 自动建 |
| 数据库 | MySQL | 数据持久化 |
| 构建 | Maven | 后端打包；前端 `npm run build` |
| 账号 | 无登录（localStorage userId） | MVP 最快出 demo |

**关键约束**
- MP **不会自动建表**，必须在 `resources/schema.sql` 提供建表语句并开启 `spring.sql.init.mode=always`。
- "一人一天一颗"由数据库唯一索引兜底，不依赖应用层单点。

---

## 8. 验收标准（MVP）

1. 后端启动后 `tiny_light` 表自动建好；
2. 未点亮时 `GET /today` 返回 `todayLighted=false`；
3. `POST /` 成功写入一条；再次同用户同日 `POST` 返回 409；
4. `GET /river?year=2026` 返回该年已点亮记录，前端渲染出金点；
5. `GET /on-this-day` 能返回往年同日记录（如有）；
6. `GET /{id}` 返回详情，不存在返回 404；
7. 前端页面可写、可看星光河、可看详情，移动端布局正常。

---

## 9. 迭代计划

- **MVP（本期）**：F1–F5，后端 MyBatis-Plus + 前端 Vue，无登录，本地可跑。
- **增强一期**：F6 连续点亮徽章、F7 年度星光河长图导出、F10 去年今日推送（`@Scheduled`）。
- **增强二期**：F8 AI 年度微光信（Spring AI）、F9 轻社区。
- **正式化**：JWT 登录、用户表、云部署（PostgreSQL + 对象存储）。

---

## 附：范围外（明确不做）
- 不做社交广场、不做即时通讯、不做多端实时同步（MVP）。
- 不做复杂情绪分析模型（MVP 仅用可选情绪标签）。
