# 今日微光（Tiny Light）需求分析文档

> 版本：v2.0 ｜ 日期：2026-08-02 ｜ 状态：MVP 已实现，白金星光河风格已落地

---

## 1. 项目概述

### 1.1 背景
人们在日常中常忽略自己经历过的微小美好，低谷时更容易陷入"我好像一直都不好"的错觉。市面上的情绪类 App 大多让人记录"今天有多糟"，而「今日微光」反其道而行--只记录"今天最亮的一刻"，把一年的好瞬间串成一条"星光河"，在用户回头看时被自己过去的光治愈。

### 1.2 产品定位（一句话）
> 每天只记一句"今天最亮的一刻"，系统把 365 天串成一条可回看的星光河；平时轻盈无负担，低谷时回头被治愈。

### 1.3 目标与价值
- **情绪价值**：正向聚焦（积极心理学"三件好事"练习的极简版），坚持成本低、幸福感收益真实。
- **留存钩子**：用得越久，星光河越有价值；"去年今日""连续点亮徽章"形成回访动力。
- **传播性**：年度星光河长图、轻社区"借到光"天然适合分享。

---

## 2. 用户分析

### 2.1 目标用户
- 想要低成本记录生活美好、但坚持不了写长日记的人；
- 有情绪记录/自我疗愈需求、偏好轻量工具的用户；
- 对"可视化、好看、能分享"有审美要求的年轻用户。

### 2.2 用户角色
| 角色 | 说明 | 当前版本 |
|---|---|---|
| 匿名访客 | 无注册，浏览器 `localStorage` 存 `userId` 区分用户 | ✅ 已支持 |
| 注册用户 | 邮箱/手机登录、多端同步 | ⏳ 后续版本 |
| 管理员 | 内容审核、社区治理 | ⏳ 轻社区时再考虑 |

> MVP 采用"无登录极简版"：不注册、不登录，最快出可玩 demo，先把核心闭环跑通。

### 2.3 典型使用场景
- **睡前 1 分钟**：写一句今天最亮的事 + 选个心情 -> 点亮当天的星。
- **低谷时**：打开星光河，看过去被点亮的日子，被治愈。
- **年末**：导出星光河长图发朋友圈；收到 AI 写给自己的"年度微光信"。
- **日常**：收到"去年今日，你写过……"的温柔提醒。

---

## 3. 功能需求

### 3.1 功能清单
| 编号 | 功能 | 优先级 | 状态 | 说明 |
|---|---|---|---|---|
| F1 | 记录今日微光 | P0 核心 | ✅ | 写一句文字 + 心情标签，点亮当天 |
| F2 | 查看今日状态 | P0 核心 | ✅ | 是否已点亮、今天写了什么 |
| F3 | 年度星光河 | P0 核心 | ✅ | SVG 贝塞尔曲线沿路径散布 365 颗星，金色=已点亮 |
| F4 | 那年今日 | P1 | ✅ | 显示近 5 年同月同日的微光（便签卡散落布局） |
| F5 | 微光详情 | P1 | ✅ | 点亮星触发涟漪 + 打开详情弹窗；越权访问返回 403 |
| F6 | 统计徽章 | P2 | ✅ | 连续/最长/今年/累计 + 心情分布（散落玻璃徽章） |
| F11 | 编辑/删除今日 | P1 | ✅ | 仅当天可改/删，需 userId 校验归属 |
| F12 | 分页历史列表 | P1 | ✅ | 按页加载全部微光记录 |
| F13 | 健康检查 | P0 | ✅ | `GET /api/health` 探活 |
| F7 | 年度星光河长图 | P2 | ⏳ | 一键生成竖版"我的 2026 星光河"长图 |
| F8 | AI 年度微光信 | P3 | ⏳ | 年末用大模型把全年星光凝成一封信 |
| F9 | 轻社区"借到光" | P3 | ⏳ | 某颗星设为公开，别人可"借到光" |
| F10 | 去年今日推送 | P3 | ⏳ | 定时任务每日推"一年前的你写道…" |

### 3.2 核心功能详述

**F1 记录今日微光**
- 用户故事：作为访客，我想写下今天最亮的一刻，这样我能把它存进我的星光河。
- 输入：`userId`（自动）、`content`（必填，≤500 字）、`mood`（必填，7 选 1）。
- 心情白名单：`开心 / 平静 / 感恩 / 疲惫 / 感动 / 思念 / 期待`。
- 规则：**一人一天只能一颗**；重复提交返回 409 冲突。
- 验收：提交后该用户当天 `todayLighted=true`；数据库新增一行；星光河对应日期出现金星。

**F2 查看今日状态**
- 用户故事：作为访客，我想知道今天是否已点亮，避免重复写。
- 验收：返回 `{todayLighted: boolean, light: LightResponse | null}`。

**F3 年度星光河**
- 用户故事：作为访客，我想看某一年的星光河，回看自己被点亮过的日子。
- 输入：`userId`、`year`。
- 验收：返回该年全部已点亮记录；前端沿贝塞尔曲线渲染 365 颗星，金色脉冲=已点亮，灰色暗淡=空白；今日星带双层脉冲环 + "TODAY" 标记。

**F4 那年今日**
- 用户故事：作为访客，我想看"历史上的今天"我写过什么，获得时间对照的温柔。
- 验收：返回近 5 年内、与今天同月同日的微光列表（可空）；前端最多展示 3 张便签卡，金色图钉散落布局。

**F5 微光详情**
- 用户故事：作为访客，我想点开某颗星，看回那天写的具体内容。
- 输入：微光 `id` + `userId`（校验归属）。
- 验收：返回该条完整内容；不存在返回 404；非本人记录返回 403。
- 交互：点击亮星触发金色涟漪扩散 + 打开玻璃态详情弹窗（ESC 可关）。

**F6 统计徽章**
- 返回 `{currentStreak, longestStreak, yearDays, totalDays, moodDistribution}`。
- 前端渲染 4 个散落玻璃徽章（连续/最长/今年/累计）+ 心情分布条。

**F11 编辑/删除今日微光**
- 规则：**仅当天的微光可编辑/删除**；需 `userId` 校验归属。
- 验收：编辑非今天/非本人返回 403；删除后当天可重新点亮。

### 3.3 扩展功能（Roadmap）
见 3.1 表格 ⏳ 项。其中 F8/F10 依赖 **Spring AI**（大模型）与 **@Scheduled**（定时任务）。

---

## 4. 非功能需求

| 类别 | 要求 | 现状 |
|---|---|---|
| 性能 | 星光河单表按日期范围检索，响应 < 200ms；首屏 < 1s | ✅ |
| 易用性 | 输入页极简 + 心情标签；零学习成本；移动端适配 | ✅ |
| 安全 | `userId` 归属校验防越权；`mood` 白名单防注入/XSS | ✅ 已实现 |
| 兼容性 | 现代浏览器（Chrome/Edge/Safari 近两版）；响应式 | ✅ |
| 可维护性 | 后端分层（Controller/Service/Mapper/DTO）；前端组件化 | ✅ |
| 可观测 | 开发期控制台打印 SQL；`GET /api/health` 探活 | ✅ |
| 动效性能 | GPU 加速 transform/opacity；rAF 节流；`prefers-reduced-motion` 兜底 | ✅ |

---

## 5. 数据需求

### 5.1 实体关系
- 核心实体 **微光（TinyLight）**，归属某个 `userId`。
- 关系：`用户(1) -- (N) 微光`。MVP 无独立用户表，`userId` 为前端生成的字符串（`user-` + 8 位随机）。

### 5.2 数据表设计（tiny_light）
| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | BIGINT | PK, AUTO_INCREMENT | 微光唯一 ID |
| user_id | VARCHAR(64) | NOT NULL | 用户标识（前端生成） |
| content | VARCHAR(500) | NOT NULL | 今日微光文字 |
| mood | VARCHAR(20) | NOT NULL | 心情标签（7 选 1，后端 @Pattern 白名单校验） |
| light_date | DATE | NOT NULL | 点亮日期 |
| created_at | DATETIME | 可空 | 创建时间 |
| - | UNIQUE(user_id, light_date) | - | 保证一人一天一颗 |

### 5.3 数据规则
- **一人一天一颗**：数据库唯一约束兜底 + 业务层捕获 `DuplicateKeyException` -> 409。
- **mood 白名单**：后端 `@Pattern(regexp = "开心|平静|感恩|疲惫|感动|思念|期待")`，存储层拒绝非法值，防 XSS/注入。
- **保留策略**：微光不软删（有情感价值）；删除今日微光为物理删除，可重新点亮。
- **字符上限**：content ≤ 500 字，前端 maxlength=200 + 后端校验双重保证。

---

## 6. 接口需求（API 规格）

基址：`http://localhost:8080/api/lights`（前端经 Vite 代理 `/api` 访问）

| 方法 | 路径 | 参数 | 返回 | 说明 |
|---|---|---|---|---|
| POST | `/` | body: `{userId, content, mood}` | `LightResponse` | 记录今日微光；重复->409；mood 非白名单->400 |
| GET | `/` | `userId`, `page?=1`, `size?=20` | `PageResponse` | 分页历史列表 |
| GET | `/today` | `userId` | `{todayLighted, light}` | 今日状态 |
| GET | `/river` | `userId`, `year` | `List<LightResponse>` | 年度星光河 |
| GET | `/on-this-day` | `userId` | `List<LightResponse>` | 那年今日（近 5 年） |
| GET | `/stats` | `userId` | `StatsResponse` | 统计 + 心情分布 |
| GET | `/{id}` | path: `id`, `userId` | `LightResponse` | 详情；不存在->404；非本人->403 |
| PUT | `/{id}` | path: `id`, body: `{userId, content, mood}`, `userId` | `LightResponse` | 编辑今天；非今天/非本人->403 |
| DELETE | `/{id}` | path: `id`, `userId` | - | 删除今天；非今天/非本人->403 |
| GET | `/api/health` | - | `{status, timestamp}` | 健康检查 |

**响应结构**
- `LightResponse`：`{id, userId, content, mood, lightDate, createdAt}`
- `StatsResponse`：`{totalDays, yearDays, currentStreak, longestStreak, moodDistribution}`
- `PageResponse`：`{page, size, total, items}`

**错误约定**：业务异常统一返回 `{error: "提示文案"}` + 对应 HTTP 状态码（400 参数错 / 403 越权 / 404 不存在 / 409 重复）。

---

## 7. 技术架构与约束

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | Vue 3.5 + Vite 8 + axios | dev 5173，代理 `/api`->8080；Geist 字体；内联 SVG 图标（无图标库） |
| 后端 | Spring Boot 4.1.0 + Java 17 | REST API + Bean Validation |
| 持久层 | **原生 MyBatis**（mybatis-spring-boot-starter 4.1.0）+ MySQL | `@Insert/@Select/@Update/@Delete` 注解；`map-underscore-to-camel-case` 自动映射 |
| 数据库 | MySQL | 表由 `schema.sql` 自动建（`spring.sql.init.mode=always`） |
| 构建 | Maven（后端）/ npm（前端） | `./mvnw spring-boot:run` / `npm run dev` |
| 账号 | 无登录（localStorage userId） | MVP 最快出 demo |

**关键约束**
- **不用 MyBatis-Plus**：3.5.17 与 Spring Boot 4.1 不兼容，改用原生 MyBatis。
- **不降级 Spring Boot**：保留 4.1.0 最新版。
- **不引入额外动画库**：纯 CSS + Canvas + SVG 实现动效，不装 GSAP/Framer Motion。
- "一人一天一颗"由数据库唯一索引兜底，不依赖应用层单点。
- `mood` 必填 + 白名单，前后端双重校验，防 XSS。

---

## 8. 视觉设计（白金星光河风格）

| 维度 | 实现 |
|---|---|
| 配色 | 深空 `#08081a` -> 紫暮 -> 白金晨曦 `#faf8f5` 渐变；金色色阶 `#c99a35`~`#fff3c4` |
| 字体 | Geist（无衬线）+ Geist Mono（等宽），Google Fonts CDN |
| 星河 | SVG 贝塞尔曲线路径，365 颗星沿路径散布（sin 波 + 确定性伪随机偏移），今日星最大带十字光芒 |
| 动效 | 150 颗闪烁星 + 60 颗 Canvas 金尘 + 流星 + 极光飘带 + 光标光晕 + 点击爆发 + 视差滚动 + 3D 卡片倾斜 + 按钮磁吸 + 滚动揭示，共 20+ 项 |
| 材料 | 玻璃态（`backdrop-filter: blur`）卡片 + 金色渐变描边 |
| 兼容 | `prefers-reduced-motion` 禁用动画；`hover:none` 触屏禁用光标光晕/磁吸/3D 倾斜 |

---

## 9. 验收标准（MVP）

1. 后端启动后 `tiny_light` 表自动建好，`GET /api/health` 返回 200；
2. 未点亮时 `GET /today` 返回 `todayLighted=false`；
3. `POST /` 成功写入；同用户同日再 `POST` 返回 409；mood 非白名单返回 400；
4. `GET /river?year=2026` 返回该年记录，前端渲染金星；今日星带脉冲环；
5. `GET /on-this-day` 返回往年同日记录；`GET /stats` 返回连续/累计/心情分布；
6. `GET /{id}` 不带/带错误 `userId` 返回 403；不存在返回 404；
7. `PUT/DELETE /{id}` 仅对今天的微光生效，非今天返回 403；
8. 前端可写、可编辑/删除今天、可看星光河、可看详情、可看统计，移动端布局正常。

---

## 10. 迭代计划

- **MVP（已完成）**：F1-F6 + F11-F13，原生 MyBatis + Vue，无登录，本地可跑。
- **增强一期**：F7 年度星光河长图导出、F10 去年今日推送（`@Scheduled`）。
- **增强二期**：F8 AI 年度微光信（Spring AI）、F9 轻社区。
- **正式化**：JWT 登录、用户表、云部署（PostgreSQL + 对象存储）。

---

## 附：范围外（明确不做）
- 不做社交广场、不做即时通讯、不做多端实时同步（MVP）。
- 不做复杂情绪分析模型（MVP 仅用 7 选 1 心情标签）。
